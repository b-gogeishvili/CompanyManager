Interval Tree, or Segment Tree is a data structure that is useful for finding
range sum queries, or range minimum queries. Finding sum, or minimum value
of an interval. This uses more space than Fenwick Tree, but it is faster.
